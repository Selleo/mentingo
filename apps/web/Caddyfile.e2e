{
    admin :2020
}

*.lms.localhost {
    handle /api/* {
        reverse_proxy http://localhost:3000
    }

    handle {
        root * ./build/client
        try_files {path} /index.html
        file_server
    }
}
